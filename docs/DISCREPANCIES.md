# DISCREPANCIES

Gate contract: `consumer=ConformanceExact and ProductionQuality`; `gate=accepted numeric divergence requires a measured impact and an active restoration path`; `defect_class=numeric or semantic divergence`; `deletion_condition=never; entries are immutable evidence records`.

Local entries are empty at truth-pack seed time. Do not create a `DISC-NNN` record from an inherited result: impacts and restoration switches must be measured on this model.

## Entry schema

```text
DISC-NNN
claim_id: <claim-id>
evidence_id: <artifacts path or id>
status: ACCEPTED | REJECTED | VOID
model_source_commit: <pinned commit>
oracle_source: <file_sha256:lines>
fixture_sha256: <sha256>
artifact_sha256: <sha256 or none>
cpu_features: <dispatched feature string>
command_env: <exact command and environment>
reference_behavior: <quoted source behavior>
our_behavior: <file:fn behavior>
kill_switch: <FTTS_* restoring reference behavior>
measured_impact: <metric, mean, and tail where applicable>
resolution: <decision>
review_date: <YYYY-MM-DD>
test_status: XFAIL | PASS
```

## Local entries

Two local divergences are recorded below: the tokenizer regex default (DISC-001) and the
f32-accumulation class covering every measured activation seam (DISC-002).

### DISC-003 optimized-default-route-vs-f32-reference

`claim_id: cli-default-optimized-route`; `evidence_id: scripts/audio_ab_report.py runs 2026-08-08/09 (scratchpad ab_out bundle) + bead comments on frankentts-54q / frankentts-p2-int8-talker-micro-upg`; `status: ACCEPTED`; `model_source_commit: 5d83992436eae1d760afd27aff78a71d676296fc`; `oracle_source: not-applicable (product-default decision, not a seam divergence)`; `fixture_sha256: not-applicable`; `artifact_sha256: none (runtime quantization)`; `cpu_features: Apple M4 Pro, FEAT_DotProd`; `command_env: ftts say (binary entrypoint sets FTTS_INT8=1, FTTS_INT8_CODEC=convnext, FTTS_FAST_SNAKE=1, FTTS_INT8_THREADS=6 unless the user set them)`; `reference_behavior: the f32 reference route, seam-parity-proven against the pinned oracle`; `our_behavior: the optimized route is the library-wide default (ftts_kernels::route::optimized_default, read by the generator, codec, and SnakeBeta switches); conformance suites pin the reference route explicitly via ftts_kernels::route::pin_reference, wired into the oracle/ladder/require_model entry points. AMENDED 2026-08-09: moved from CLI-entry env defaults to library-wide defaults at owner direction. AMENDED again 2026-08-09: the codec int8 arm was superseded by the BLAS-form f32 dense route (PERF-004 — faster AND closer to the oracle), so the codec is no longer quantized by default; FTTS_INT8_CODEC arms remain opt-in`; `kill_switch: FTTS_INT8=0 (master; restores the reference route end to end)`; `measured_impact: codec convnext arm LSD 0.65 dB mean / mel 0.12 dB (transparent, same-codes); fast snake SNR 129.6 dB (transparent); talker/micro W8A8 shifts the sampled code distribution (enrolled synthetic voice, seed 0: whole-utterance RMS 0.019 to 0.221, centroid 860 to 236 Hz, audible LF drone risk) — Contract-B listening with a real voice is OPEN and this entry must be revisited by it`; `resolution: owner directive 2026-08-09 — speed-first default with a one-variable reference restoration; the sampled-path quality gate remains an open obligation, not waived`; `review_date: 2026-09-09`; `test_status: PASS (cli tests; parity suites unaffected by design)`.

### DISC-005 end-of-utterance-tail-trim-on-file-sinks

`claim_id: audio-tail-trim`; `evidence_id: crates/ftts-core/src/audio.rs (trailing_noise_samples + WavWriter hold-back) with tail_tests/holdback_tests/f32_silence_tests; measurements 2026-08-10 over nine synthesized utterances plus two real recordings`; `status: ACCEPTED`; `model_source_commit: 5d83992436eae1d760afd27aff78a71d676296fc`; `oracle_source: not-applicable (output-boundary post-process; no seam of the reference forward is touched)`; `fixture_sha256: not-applicable`; `artifact_sha256: not-applicable (runtime, post-decode)`; `cpu_features: not-applicable (scalar analysis over decoded PCM)`; `command_env: ftts say / ftts make-video with a file output; NOT applied to --stream raw`; `reference_behavior: the model emits a short burst of low-level broadband noise after speech decays and before its own trailing silence, and the reference reproduces it — an interleaved A/B on identical text and seed showed the f32 REFERENCE route producing MORE of it (sustained ~90 ms, first-difference ratio up to 0.94) than the int8 route (one 10 ms window at 0.19). This is therefore NOT a quantization artifact and DISC-003 is not its cause`; `our_behavior: file sinks drop a contiguous run at the end that is simultaneously (a) below 0.15 of the utterance's own peak 10 ms window, (b) first-difference-energy-dominated above 0.25, and (c) below 0.10 of that peak in run mean; capped at 250 ms. Trailing silence is preserved and is defined by AUDIBILITY (half an i16 step), never by an exact zero, because the engine's f32 tail is tiny non-zero values that only round to zero on write. The streaming writer holds the last 250 ms back to make the decision and flushes it UNTRIMMED if dropped without finish, preserving the partial-output promise`; `kill_switch: FTTS_TRIM_TAIL=0`; `measured_impact: artifact population RMS 20-128 against speech 500-1640 and first-difference ratio 0.31-1.92 against voiced speech 0.005-0.10 — two cleanly separated populations with an empty gap, which is what the thresholds sit in. Observed removal 30-190 ms across synthesized samples; 40 ms on the end-to-end verified case, matching the offline detector exactly. FALSE-POSITIVE EVIDENCE: two real human recordings (a 3.5-minute enrollment take and a denoise test file) trim 0, and clean audio is byte-identical through the hold-back at every packet size`; `resolution: accepted as an output-boundary cosmetic trim with a one-variable restoration. It is deliberately NOT in the engine or any parity path: conformance compares codes and decoded PCM, which this never sees. NOT CLAIMED: that the remaining tail is inaudible — the thresholds are conservative by design and an owner listening pass is an open obligation, tracked separately`; `review_date: 2026-11-10`; `test_status: PASS (70 ftts-core tests incl. a regression test that fails against the parameter-shadowing bug, one against exact-zero silence detection, and one proving a dropped writer keeps every sample it accepted)`.

### DISC-006 browser-diverges-from-the-cli-after-frame-0

`claim_id: wasm-cli-token-stream-equality`; `evidence_id: site/harness/browser.mjs --cli-golden runs 2026-08-23 and 2026-08-24 (reproduced identically; WAV artifacts hashed below) + bead frankentts-rc-disc006-wasm-divergence-4oqv comments`; `status: ACCEPTED (carried divergence under active investigation — frankentts-rc-disc006-wasm-divergence-4oqv)`; `model_source_commit: 5d83992436eae1d760afd27aff78a71d676296fc`; `oracle_source: not-applicable (cross-target determinism claim, not a seam vs oracle)`; `fixture_sha256: cli golden wav 33cda600f287597de2d9b61cf80e94ee5fb3b5645c40980fc6ff207cc24445f6`; `artifact_sha256: browser pcm wav 8d51fb0f94ed0db1b13491396058bbdbaa85568d6433afb539e459f35de7db3a`; `cpu_features: native neon-sdot vs wasm32 simd128 int8 tier`; `command_env: node site/harness/browser.mjs --cli-golden <wav> --pcm-out <wav>; golden: ftts say "The quick brown fox jumps over the lazy dog." --voice matt --seed 0 (release, macOS arm64, Accelerate SGEMM codec route)`; `reference_behavior: the CLI's canonical rendering of the pinned inputs (41 frames)`; `our_behavior: crates/ftts-wasm render — frame 0 exact to 1919/1920 samples @75.0 dB SNR (max |diff| 1 i16 LSB, first differing sample 1873), then unrelated from frame 1 (0.6 dB); browser 40 frames vs CLI 41; whole utterance 2.61% identical, SNR −4.6 dB`; `kill_switch: none yet — restoration requires cross-target canonical routes (see resolution)`; `measured_impact: audio differs from the first second onward between targets for the same seed; both renditions are valid draws of the production sampler; no user-visible degradation on either target alone`; `resolution: carry as ledgered divergence. Two seams: (A) codec dense projections — native Accelerate cblas_sgemm vs wasm scalar reference loop, same codes, reduction-order epsilon ≤1 LSB (frame 0); (B) a per-target f32 epsilon reaches the sampler and flips one draw by frame 1 (codes[1] differ ⇒ 40 vs 41 frames). Elimination needs cross-target-pinned f32 reductions/transcendentals in the shared talker path plus an equality gate; tracked as the follow-up instrumentation bead`; `review_date: 2026-11-22`; `test_status: XFAIL`

> Schema note 2026-08-24: this entry originally carried `status: OPEN` and free-form fields,
> outside the register vocabulary above. Rewritten into schema during the reality-check audit;
> measurement prose below is unchanged evidence.

**What is measured.** Same text, voice matt, seed 0, inputs read back and asserted. Frame 0 is 1919/1920 samples identical at 75.0 dB SNR. Frame 1 is 0.6 dB. Whole utterance 2.61% identical, and the browser emits 40 frames where the CLI emits 41. Reproduced unchanged on 2026-08-24 and again 2026-08-25 at current HEAD with freshly built site/pkg and a fresh native golden (numbers identical to the decimal).

**What the seam taps added (2026-08-25, frankentts-p16p).** Generator-side taps (`FTTS_DEBUG_TAPS`, file sink) hash the tensors crossing each seam; the wasm twin emits the same lines to its console (`--debug-taps --transcript-out`). Result at current HEAD: the PREFILL output already diverges — `ph`/`plog` hashes differ before any frame is generated — so the per-target f32 epsilon lives in the shared talker prompt-forward compiled per target, exactly where the int8-exact path gives no coverage. Early draws usually survive the epsilon (frame 0's primary matched here), and one flip cascades to unrelated renderings and the 40-vs-41 frame count.

**Why one epsilon is enough.** The talker SAMPLES — `do_sample=true`, T=0.9, top_k=50, sixteen draws per frame — and the CLI's own note scopes determinism to "build + ISA + sampler version + seed". So any difference that reaches the logits flips a draw, and from the next frame on the two renderings are unrelated. This is not a degradation; both are valid draws. It is still a defect, because the same seed should give the same audio everywhere and nothing in this pipeline is lossy.

**Ruled out by measurement, not by reading.**
* The codec's int8 ConvNeXt arm, which used to be armed on wasm only — fixing that moved frame 0 from 232/1920 to 1919/1920 and is what made the remaining epsilon visible at all.
* f32 accumulation lanes (wasm used 8 partial chains, native 1). Pinned to the native order; output did not move (307 vs 305 identical samples).
* Preset vectors. The shipped iOS framework carries byte-identical `judy.spk` and `matt.spk`; `read_spk` and `preset_vector` perform the same raw f32-LE read.
* Tokenization. Browser wraps 18 tokens; the CLI reports `prepared_token_count: 18`.
* The cold-embedding row supply (this session's own change), by the same token count.
* Every native route knob: `FTTS_INT8` on/off, `FTTS_INT8_CODEC` on/off, `--math-mode strict/fast` are all bit-identical natively at 41 frames. Native produces one canonical result; only wasm moves.

**Where it must be.** Two seams, not one. (A) Frame 0's single differing sample is codec arithmetic, not the talker: codes agree there, and a 1-LSB i16 flip at sample 1873 is the signature of the dense-projection route split — native runs `cblas_sgemm` through the default `accelerate-sgemm` feature (`crates/ftts-kernels/src/f32ref.rs`), while wasm has no Accelerate and falls to the scalar reference loop. Same math, different reduction order. (B) From frame 1 the renderings are unrelated while seam A's scale is 1 LSB, so codes[1] must differ: an f32 difference in the shared talker path survives frame 0's draws and flips one draw at frame 1. The candidates are everything the exact-i32 int8 path does not pin per target: RMSNorm sums, attention softmax, the codec-head logits projection, and the transcendental implementations (exp/sin/cos), which are libm-backed and differ by ulps across wasm32 and macOS.

**What closing it requires.** Intermediate-tensor checksums dumped from BOTH targets at the same seam; codes locate nothing (they agree at frame 0 by construction and disagree at frame 1 as a consequence). A generator-side codes tap now exists for this (`FTTS_DEBUG_CODES=1`, symmetric prints `ftts-cli codes[N]` / `ftts-wasm codes[N]`, added 2026-08-24 after the earlier sink-side tap was removed for deadness); it confirms the seam-B location but not its operator. The next rung is a checksum tap on RMSNorm/softmax/logits inputs in both engines, then pinning or canonically-routing whichever op moves.

### DISC-004 browser-cache-endpoint-verification-vs-full-digest

`claim_id: wasm-cached-model-verification`; `evidence_id: site/model-manifest.js endpoint digests, cross-checked against the full manifest digests on the pinned local artifacts 2026-08-09 (both full SHA-256 reproduced exactly: fttsq 597f7eb3…, codec 836b7b35…)`; `status: ACCEPTED`; `model_source_commit: 5d83992436eae1d760afd27aff78a71d676296fc`; `oracle_source: not-applicable (browser cache-integrity policy, not a numerical seam)`; `fixture_sha256: not-applicable`; `artifact_sha256: qwen3-tts-12hz-0.6b-base.fttsq=597f7eb3314a2fe5be74fa10a6a3a28ace9e10e582c641deccd37348a0ccd824, speech_tokenizer_model.safetensors=836b7b357f5ea43e889936a3709af68dfe3751881acefe4ecf0dbd30ba571258`; `cpu_features: not-applicable (JS/WebCrypto)`; `command_env: browser page load against site/ with a warm OPFS cache`; `reference_behavior: every cached asset is re-proved with a full-file SHA-256 before the engine sees it`; `our_behavior: site/loader.js endpointsMatch() proves a cached asset with exact byte length plus SHA-256 over the first and last 10 MiB; a FRESH download is still fully verified, hashed from the bytes as they stream past at no extra cost, and a RESUMED download still takes the full digest because its prefix hash state is gone`; `kill_switch: ?fullverify (URL parameter; restores the whole-file digest on every cached asset)`; `measured_impact: bytes hashed on a warm-cache load drops from 1,994 MB to 20 MB (99.0% less) across the two large assets; the endpoint windows also move from the JS SHA-256 core (~193 MB/s measured on iPhone) to crypto.subtle. NOT A SECURITY EQUIVALENCE: this detects truncation, short or failed writes, partially-overwritten files, stale-manifest mismatch, and wrong-file substitution — the failure modes OPFS produces — but does NOT detect a targeted modification confined to the file interior. The bytes were fully verified at download time; this re-proves the file is unchanged, not that an attacker with OPFS write access left the middle alone`; `resolution: owner directive 2026-08-09 — full-file rehash on every visit was the dominant warm-start cost on desktop and a tab-kill risk on iOS; endpoint+length is the accepted cache-integrity check with a one-parameter restoration of the strict path`; `review_date: 2026-11-09`; `test_status: PASS (site/sha256.test.js pins digestRange against crypto.subtle; JS core vs WebCrypto agreement over 17 lengths and 7 slice boundaries)`.

### DISC-001 tokenizer-regex-official-vs-native

`claim_id: tokenizer-regex-official-vs-native`; `evidence_id: docs/truth-pack/tokenizer/tokenizer_conformance.json`; `status: ACCEPTED`; `model_source_commit: 5d83992436eae1d760afd27aff78a71d676296fc`; `oracle_source: docs/truth-pack/tokenizer/OQ11_TOKENIZER.md:72-148`; `fixture_sha256: 78f56d6ab68f2a1927ab33a37497908b722d4d8d77df47ed43149a4fbfeec99a`; `artifact_sha256: vocab.json=ca10d7e9fb3ed18575dd1e277a2579c16d108e32f27439684afa0e10b1440910, merges.txt=599bab54075088774b1733fde865d5bd747cbcc7a547c5bc12610e874e26f5e3, tokenizer_config.json=dc3c31c3bdaedd5016382bb3cbe07323026775ad51f5a4fb564505992ae4a670`; `cpu_features: not-applicable (pure Rust tokenization)`; `command_env: CARGO_TARGET_DIR=/tmp/frankentts-tokenizer-target cargo test -p ftts-model-qwen --no-fail-fast`; `reference_behavior: the official Qwen3-TTS entrypoints load Qwen2TokenizerFast with fix_mistral_regex=True`; `our_behavior: crates/ftts-model-qwen/src/tokenizer.rs:13-14 and :28-39 default to the official Mistral expression, with the native Qwen expression only behind the explicit switch`; `kill_switch: FTTS_TOKENIZER_REGEX=native`; `measured_impact: 6/92 pinned corpus cases change token ids under native mode (five mixed-case identifier cases and Thai); all 92 official ids and their NFC decode values pass`; `resolution: retain official as the conformance default until a separate listening experiment evaluates the native alternative`; `review_date: 2026-08-06`; `test_status: PASS`.

> Re-review 2026-08-24 (reality-check audit; original review date 2026-08-06 had lapsed):
> decision stands — the official regex remains the conformance default and
> `FTTS_TOKENIZER_REGEX=native` stays available; 92/92 pinned corpus ids still pass under the
> default. Next review: 2026-09-30.

### DISC-002 f32-accumulation-vs-accelerate-sleef-stack

`claim_id: f32-accumulation-vs-accelerate-sleef-stack`; `evidence_id: bead frankentts-p1-talker-z2w comments (commits e481fa8, 4c8a64a, a7d4cef, b41213f, 203ec22) + crates/ftts-conformance/tests/{talker_layer_l2,talker_all_layers_l2,microdecoder_layer_l2,microdecoder_head_l2,talker_argmax_l3}.rs`; `status: ACCEPTED`; `model_source_commit: 5d83992436eae1d760afd27aff78a71d676296fc`; `oracle_source: ft7-cpu-fp32-r1 capture environment — Torch 2.7.1 aarch64, BLAS_INFO=accelerate, AT_BUILD_ARM_VEC256_WITH_SLEEF, intra-op threads 10`; `fixture_sha256: 5ec2bc3f3217f9e026198c0694b3993d9911f7e954ea726c69ebd95e7d5ba4dd (fixture_manifest.json)`; `artifact_sha256: not-applicable (no converted artifact involved; f32 reference forward vs oracle activations)`; `cpu_features: apple-m-series aarch64 (subject scalar f32; oracle Accelerate SGEMM + SLEEF Vec256)`; `command_env: CARGO_TARGET_DIR=<private> cargo test --locked -p ftts-conformance --test talker_all_layers_l2 --test talker_argmax_l3`; `reference_behavior: the pinned oracle evaluates matmul through Accelerate SGEMM (blocked/vectorized reduction order) and rsqrt/exp through SLEEF vector routines — ~1-ulp, not correctly rounded`; `our_behavior: scalar f32 loops with LLVM autovectorization; different-but-equally-valid reduction order and correctly-rounded scalar transcendentals`; `kill_switch: none can restore bit-identity — the divergence is inherent to any reduction order that does not bit-mirror the oracle's BLAS; the cargo feature accelerate-sgemm (a7d4cef) narrows layer-00 max_abs 9.54e-6 → 1.43e-6 without closing it, and stays off by default`; `measured_impact: four independent seam families all inside the sqrt(K)*eps f32 accumulation budget — talker layer 00 max_abs 9.537e-6 (5.83e-7 relative, worst of all 28 layers), microdecoder head 0.72x budget, microdecoder layers 00..04 0.05x budget, codec seams per codec_decode_l2 — cosine ≥ 0.999999999999 everywhere; full 28-layer accumulated talker stack 1.78e-6 relative; DISCRETE ARTIFACTS UNAFFECTED: primary-code argmax exact at the head seam and through the full accumulated stack (talker_argmax_l3), whole-utterance codes exact through the engine (engine_e2e_ft7), sampler-over-oracle-logits exact (ladder L4)`; `resolution: Contract-A CPU-tier acceptance for CONTINUOUS activation seams is a scale-relative bound 1e-4 + cosine floor 0.999999 (wiring errors are O(1) relative and collapse cosine — separated from rounding by three decades); DISCRETE rungs (L0 ids, argmax, L4 codes) remain EXACT with zero tolerance; the nondeterminism-floor's max_abs 0.0 remains correct as the ORACLE'S self-noise but is not attainable subject-vs-oracle across differing BLAS stacks`; `review_date: 2026-09-07`; `test_status: PASS (argmax/codes exact; activation seams green under the bound)`.

## Inherited priors (pre-truth-pack)

None imported as discrepancy records. Sibling numerical outcomes are hypotheses only; add a `DISC-NNN` only after this project's pinned source, fixture, impact, and `FTTS_*` restoration switch exist.
