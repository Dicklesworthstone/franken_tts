[package]
name = "ftts-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
repository.workspace = true
description = "franken_tts browser bindings: the Qwen3-TTS pipeline compiled to WASM for the playground"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ftts-artifacts = { path = "../ftts-artifacts", version = "0.1.10" }
ftts-core = { path = "../ftts-core", version = "0.1.10" }
ftts-kernels = { path = "../ftts-kernels", version = "0.1.10" }
ftts-model-qwen = { path = "../ftts-model-qwen", version = "0.1.10" }
js-sys = "0.3"
serde_json = "1.0"
wasm-bindgen = "0.2"

[lints]
workspace = true
