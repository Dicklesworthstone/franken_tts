[package]
name = "ftts-model-qwen"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
repository.workspace = true
description = "Qwen3-TTS-12Hz-0.6B model graph for franken_tts: talker, microdecoder, codec, tokenizer, sampler"
keywords = ["tts", "speech", "qwen", "voice", "audio"]
categories = ["multimedia::audio"]

[dependencies]
fancy-regex = "0.18"
ftts-artifacts = { path = "../ftts-artifacts", version = "0.1.2" }
ftts-core = { path = "../ftts-core", version = "0.1.2" }
ftts-kernels = { path = "../ftts-kernels", version = "0.1.2" }
rustfft = "6.2"
serde_json = "1.0"
unicode-normalization = "0.1"

[lints]
workspace = true
